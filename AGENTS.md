# Agent Instructions

## Rules

> Adhere to these rules unless told otherwise.

- Make a commit and push after each change.
- Never sleep longer than 1 second: either don't use `sleep` at all, or at most `sleep 1`.
